*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Per-module build outputs
**/build/

# Gradle wrapper jar is regenerated by `gradle wrapper`; not committed here.
gradle/wrapper/gradle-wrapper.jar

# Local release-signing keystore + its base64 encoding — see
# scripts/generate-release-keystore.sh. Never commit this. The script
# defaults to writing outside any repo, but this is a safety net in case
# it's ever run from inside this one.
.signing/
game-space-signing/
*.jks
*.keystore
