# Add project-specific ProGuard/R8 rules here as features are added.
# Hilt, Room and kotlinx.serialization each ship their own consumer rules
# via their AARs, so no manual keep rules are required for them yet.
